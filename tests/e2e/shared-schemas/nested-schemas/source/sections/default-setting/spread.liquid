{# this should return 2 object settings }
{# the first object should be a text type }
{# the second object should be a textarea type }

{% schema %}
  {
    "name": "Default Spread",
    "settings": [
      {
        "$ref": "default-setting-spread.basic"
      }
    ]
  }
{% endschema %}