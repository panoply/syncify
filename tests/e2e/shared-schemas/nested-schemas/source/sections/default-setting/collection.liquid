{# this should return 1 object settings }
{# the object should be a text type }

{% schema %}
  {
    "name": "Default Collection",
    "settings": [
      {
        "$ref": "default-setting-collection.basic"
      }
    ]
  }
{% endschema %}