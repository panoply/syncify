{# this references a block singleton }
{# it returns a single block object with hard coded settings }

{% schema %}
  {
    "name": "Default Block - Singleton",
    "blocks": [
      {
        "$ref": "default-block-singleton.text"
      }
    ]
  }
{% endschema %}