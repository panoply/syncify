import { Bundle, Config } from 'types';

export default (options: {}) => {

  return {
    name: '',
    onInit (config: Config) {

    },
    onWarn (warning: any) {

    },
    onTransform (file: File) {

    },
    onReload (dom: Document) {

    }
  };
};
