{
  "name": "@syncify/live",
  "version": "1.0.0",
  "description": "Syncify live reload module.",
  "main": "index.js",
  "scripts": {
    "dev": "esbuild src/embed.ts --outfile=index.js --format=esm --bundle --watch",
    "build": "pnpm run prod && gzip-size index.js --include-original",
    "prod": "esbuild src/embed.ts --minify --tree-shaking=true --format=iife --bundle --outfile=index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": {
    "name": "Nikolas Savvidis",
    "url": "https://github.com/panoply"
  },
  "license": "MIT",
  "dependencies": {
    "mithril": "^2.2.2"
  },
  "devDependencies": {
    "esbuild": "^0.15.1"
  }
}
